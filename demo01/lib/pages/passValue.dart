import 'package:flutter/material.dart';


class PassValue extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("passValue"),
      ),
      body:Center(
      
      )
    );
  }
}
